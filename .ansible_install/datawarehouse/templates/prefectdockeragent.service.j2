[Unit]
Description=Prefect Docker Agent

[Service]
Type=simple

User={{ user_application }}
ExecStart=/opt/docker/{{ application }}/prefect-agent/prefectdockeragent.sh
Restart=always
RestartSec=30

[Install]
WantedBy=multi-user.target
